import 'package:flutter/material.dart';
import 'common_widgets.dart';

class FavouritesPage extends StatelessWidget {
  @override
    Widget build(BuildContext context) {
      return Scaffold(
        appBar: ootmAppBar("Ulubione", false),
        body: Text("Ulubione")
      );
    }
}